import * as React from 'react';
import { SvgUri } from 'react-native-svg';

const Georgia = (props: any) => (
	<SvgUri
		width={50}
		height={50}
		uri="https://api.iconify.design/circle-flags:ge.svg"
		{...props}
	/>
);
export default Georgia;
